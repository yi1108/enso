# Enso 构建的实际解决方案

由于 Enso 项目的复杂性，以下是三种可行的获取安装包的方式：

## ✅ 方案 1：使用官方预构建版本（推荐）

### 最简单、最可靠

```powershell
# 直接访问官方网站下载
# https://github.com/enso-org/enso/releases

# 或使用浏览器打开
start https://github.com/enso-org/enso/releases
```

下载后直接安装即可，无需构建。

---

## ✅ 方案 2：使用 GitHub Actions 自动构建

利用项目的 CI/CD 系统自动构建：

### 2.1 Fork 项目到你的账号

1. 访问 https://github.com/enso-org/enso
2. 点击 "Fork" 按钮
3. 选择 Fork 到你的账号

### 2.2 触发构建

有两种方式：

**方式 A：创建 Release 触发构建**

1. 进入你 Fork 的项目
2. 点击 "Releases"
3. 点击 "Draft a new release"
4. 创建一个标签（如 `v0.2.1-custom`）
5. 发布后会自动触发 CI 构建

**方式 B：启用 GitHub Actions 手动触发**

1. 进入你的 Fork 项目
2. 点击 "Actions" 标签
3. 选择 "Nightly" 或 "IDE Packaging" workflow
4. 点击 "Run workflow"
5. 等待构建完成（约 30-60 分钟）

### 2.3 下载构建产物

1. 构建完成后，进入 Actions 页面
2. 点击对应的 workflow run
3. 在 "Artifacts" 部分下载构建产物

---

## ✅ 方案 3：本地简化构建（部分功能）

如果只需要 Engine 服务（不需要 GUI），可以尝试简化构建：

```powershell
cd C:\Users\L1822\IdeaProjects\enso

# 安装基础依赖
corepack pnpm install

# 尝试构建（不需要完整环境）
.\run backend get
```

这可能会失败，因为需要 GraalVM 和 Rust。

---

## ⚠️ Docker 构建的挑战

Enso 使用复杂的构建系统：
1. **GraalVM JDK 25.0.1** - 需要特定版本
2. **Scala/Rust 混合编译** - Bazel + sbt + Cargo
3. **Windows 特定工具** - Visual Studio C++ 编译器
4. **长时间构建** - 首次构建 1-2 小时
5. **大文件下载** - Maven/Cargo/npm 依赖

### 为什么 Docker 不太适合？

1. ❌ 基础镜像问题（Oracle Linux vs Debian）
2. ❌ 跨平台构建限制（Linux 容器构建 Windows 应用）
3. ❌ Electron 打包需要 Windows 主机
4. ❌ 构建时间过长，不适合频繁 Docker 构建

---

## 🎯 推荐方案对比

| 方案 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| 官方预构建版本 | ✅ 快速<br>✅ 可靠<br>✅ 无需构建 | ❌ 无法修改代码 | ⭐⭐⭐⭐⭐ |
| GitHub Actions | ✅ 自动化<br>✅ 免费<br>✅ 可定制 | ⏱️ 首次需配置 | ⭐⭐⭐⭐ |
| 本地完整构建 | ✅ 完全控制 | ❌ 配置复杂<br>❌ 耗时长 | ⭐⭐ |
| Docker 构建 | ✅ 环境隔离 | ❌ 跨平台问题<br>❌ 配置困难 | ⭐ |

---

## 🚀 立即可用的方案

### 现在就可以做：

1. **下载官方版本**
   ```powershell
   start https://github.com/enso-org/enso/releases
   ```

2. **使用 Nightly Build**（最新功能）
   ```powershell
   start https://github.com/enso-org/enso/actions/workflows/Engine%20CI
   ```

3. **查看已有的构建产物**
   - 检查 `dist/` 目录是否已有构建
   - 检查项目 Release 页面

---

## 💡 如果你想自定义构建

### 最简单的方式：修改后重新构建

1. **克隆项目到你的账号**
   ```powershell
   # Fork enso-org/enso 到你的账号
   git clone https://github.com/你的账号/enso
   cd enso
   ```

2. **进行修改**（如汉化）
   ```powershell
   # 已完成的修改
   git status
   ```

3. **推送到你的 Fork**
   ```powershell
   git push origin main
   ```

4. **在 GitHub 上创建 Release**
   - 这会自动触发 CI 构建
   - 构建完成后下载产物

---

## 📞 需要帮助？

如果需要：
- 构建特定的功能
- 调试构建问题
- 了解构建系统

请参考：
- [CONTRIBUTING.md](docs/CONTRIBUTING.md) - 官方贡献指南
- [Getting Enso](docs/getting-enso.md) - 获取指南
- [安装指南.md](安装指南.md) - 我们创建的安装指南

---

## 总结

**最实际的方式**：
1. ✅ 使用官方预构建版本（立即可用）
2. ✅ Fork 后用 GitHub Actions 构建（自定义）
3. ⚠️ 本地完整构建（仅用于深度开发）
4. ❌ Docker 构建（技术限制较多）

需要我帮你设置 GitHub Actions 自动构建吗？
