# 玉皇大帝·数据分析平台 - 安装指南

本文档提供在 Windows 环境下安装和运行玉皇大帝·数据分析平台（Enso）的完整指南。

## 目录

- [系统要求](#系统要求)
- [快速开始（预构建版本）](#快速开始预构建版本)
- [完整开发环境搭建](#完整开发环境搭建)
- [构建项目](#构建项目)
- [运行项目](#运行项目)
- [常见问题](#常见问题)

---

## 系统要求

### 最低配置

- **操作系统**: Windows 10/11 (64位)
- **处理器**: x86_64 架构
- **内存**: 至少 8GB RAM（推荐 16GB）
- **磁盘空间**: 至少 10GB 可用空间

### 支持的开发环境

- **操作系统**: Windows 10, macOS 10.14+, Linux 4.4+
- **架构**: x86_64 (所有平台), arm64 (仅 Mac)

---

## 快速开始（预构建版本）

如果您只想使用玉皇大帝·数据分析平台，而不需要开发，推荐使用预构建版本：

### 方式 1：下载官方发布版本

1. 访问 [Enso Releases](https://github.com/enso-org/enso/releases)
2. 下载最新版本的安装包：
   - Windows: `enso-setup-<version>.exe`
   - macOS: `Enso-<version>.dmg`
   - Linux: `enso-<version>.AppImage`

3. 运行安装程序并按照提示完成安装

4. 启动应用：
   - Windows: 从开始菜单或桌面快捷方式启动
   - macOS: 从 Applications 文件夹启动
   - Linux: 运行下载的 AppImage 文件

### 方式 2：使用每日构建版本

获取最新功能（可能不稳定）：

1. 访问 [GitHub Actions - Engine CI](https://github.com/enso-org/enso/actions?query=workflow%3A%22Engine+CI%22+branch%3Amain)
2. 点击最新的构建任务
3. 在 "Artifacts" 部分下载 `enso-engine-<version>` 包
4. 解压并运行 `bin/enso.bat` (Windows) 或 `bin/enso` (Unix)

---

## 完整开发环境搭建

如果您需要修改源代码或参与开发，请按照以下步骤搭建完整开发环境。

### 1. 安装基础工具

#### 1.1 启用 Windows 开发者模式

**必需**：支持创建文件系统符号链接。

1. 打开 **设置** > **更新和安全** > **开发者选项**
2. 启用 **开发人员模式**
3. 或使用 PowerShell（管理员）：
   ```powershell
   reg add "HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\AppModelUnlock" /v AllowDevelopmentWithoutDevLicense /t REG_DWORD /d 1
   ```

#### 1.2 安装 Git

下载并安装 [Git for Windows](https://git-scm.com/download/win)

验证安装：
```powershell
git --version
```

#### 1.3 安装 Node.js

1. 下载并安装 [Node.js LTS 版本](https://nodejs.org/)
2. 推荐使用 [fnm](https://github.com/Schniz/fnm) 或 [nvm-windows](https://github.com/coreybutler/nvm-windows) 管理多个版本

验证安装：
```powershell
node --version
# 应输出: v22.11.0 或更高
```

#### 1.4 安装 pnpm

```powershell
# 使用 corepack（推荐）
corepack enable
corepack prepare pnpm@10.2.1 --activate

# 或使用 npm
npm install -g pnpm@10.2.1
```

验证安装：
```powershell
pnpm --version
# 应输出: 10.2.1 或更高
```

### 2. 安装 Java 开发环境

#### 2.1 安装 GraalVM

**重要**: Enso 需要 **GraalVM JDK 25.0.1**（Java 21 构建）

1. 访问 [GraalVM CE Releases](https://github.com/graalvm/graalvm-ce-builds/releases)
2. 下载 `graalvm-ce-java21-25.0.1` 对应平台的版本
3. 解压到目录，例如：`C:\GraalVM`

#### 2.2 配置 JAVA_HOME

```powershell
# 设置系统环境变量
[System.Environment]::SetEnvironmentVariable("JAVA_HOME", "C:\GraalVM", "Machine")

# 添加到 PATH
$env:PATH = "C:\GraalVM\bin;$env:PATH"
```

或在系统设置中：
1. 右键 **此电脑** > **属性** > **高级系统设置**
2. 点击 **环境变量**
3. 新建系统变量 `JAVA_HOME` = `C:\GraalVM`
4. 编辑 `Path` 变量，添加 `%JAVA_HOME%\bin`

验证安装：
```powershell
java -version
# 应输出类似:
# openjdk version "25.0.1" <date>
# OpenJDK Runtime Environment (build 25.0.1+<build>)
# OpenJDK 64-Bit Server VM (build 25.0.1+<build>, mixed mode)
```

### 3. 安装 Scala 构建工具

#### 3.1 安装 sbt

1. 下载 [sbt 安装程序](https://www.scala-sbt.org/download.html)
2. 运行安装程序，默认路径安装

验证安装：
```powershell
sbt --version
```

#### 3.2 安装 Maven

1. 下载 [Apache Maven](https://maven.apache.org/download.cgi)
2. 解压到目录，例如：`C:\Apache\maven`
3. 添加到 PATH：`C:\Apache\maven\bin`

验证安装：
```powershell
mvn --version
# 应输出: Apache Maven 3.6.3 或更高
```

### 4. 安装 Rust 工具链

#### 4.1 安装 Rustup

1. 访问 [Rustup](https://rustup.rs/)
2. 下载并运行 `rustup-init.exe`
3. 按照提示完成安装

验证安装：
```powershell
rustc --version
cargo --version
```

### 5. 安装 C++ 编译器（可选）

如果需要构建 Native Image：

1. 安装 [Visual Studio Community](https://visualstudio.microsoft.com/vs/community/)
2. 在安装时选择 **"使用 C++ 的桌面开发"** 工作负载
3. 配置 x64 Native Tools 命令行

### 6. 安装 Bazel（可选）

Bazel 是主要构建系统，但如果未安装，构建脚本会自动使用 Cargo。

1. 下载 [Bazelisk](https://github.com/bazelbuild/bazelisk/releases)（自动下载正确版本的 Bazel）
2. 将可执行文件重命名为 `bazel.exe`
3. 放到 PATH 中的目录

验证安装：
```powershell
bazel --version
```

### 7. 配置 Bazel（仅 Windows）

创建 `%USERPROFILE%\.bazelrc` 文件（或在项目根目录创建 `.bazelrc.local`），包含：

```
build --enable_runfiles
```

这是 Windows 上运行测试所需的配置。

---

## 构建项目

### 1. 克隆项目

```powershell
# 如果尚未克隆
git clone https://www.gitpp.com/liyihui/enso
cd enso
```

### 2. 安装依赖

```powershell
# 安装 Node.js 依赖
corepack pnpm install
```

### 3. 构建选项

#### 方式 1：使用项目构建脚本（推荐）

```powershell
# Windows PowerShell
.\run.ps1 build

# 或 CMD
run.cmd build
```

#### 方式 2：使用 Bazel

```powershell
bazel build //...
```

#### 方式 3：使用 Cargo

```powershell
cargo build -p enso-build-cli
```

### 4. 构建 IDE

```powershell
# 编译 TypeScript
corepack pnpm run compile

# 构建 GUI
corepack pnpm run build:gui

# 构建 IDE
corepack pnpm run build:ide
```

---

## 运行项目

### 开发模式

#### 启动 GUI 开发服务器

```powershell
cd app\gui
pnpm run dev:vite
```

访问：`http://localhost:5173`

#### 启动 Electron 开发模式

```powershell
# 在项目根目录
corepack pnpm run dev:gui
```

### 生产模式

#### 运行构建的 IDE

```powershell
# 打包应用
corepack pnpm run dist:ide

# 输出目录
# distribution/ide-desktop/
```

#### 运行 Engine 服务

```powershell
# 使用项目脚本
.\run.ps1 --server --daemon

# 或直接运行
.\bazel-bin\build_tools\cli\enso_build_cli_bin.exe --server --daemon
```

Engine 服务默认端口：
- RPC: 30001
- DATA: 30002
- YDOC: 5976

---

## 常见问题

### 1. Java 版本错误

**问题**: `Could not find option with name enso-runtime-server.enable`

**原因**: Java 版本不匹配

**解决**:
1. 检查 Java 版本：`java -version`
2. 确保使用 GraalVM JDK 25.0.1
3. 检查 `JAVA_HOME` 环境变量

### 2. 符号链接错误

**问题**: `无法创建文件，因为您没有足够的权限`

**原因**: Windows 开发者模式未启用

**解决**:
1. 启用开发者模式（见上文）
2. 或以管理员身份运行 PowerShell

### 3. pnpm 版本不匹配

**问题**: `ERROR: This project requires pnpm 10.2.1`

**解决**:
```powershell
corepack enable
corepack prepare pnpm@10.2.1 --activate
```

### 4. 构建失败 - 缺少依赖

**问题**: Bazel 或 Cargo 构建失败

**解决**:
```powershell
# 清理缓存
bazel clean --expunge_async

# 或重新安装依赖
corepack pnpm install --force
```

### 5. Electron 启动失败

**问题**: Electron 安装被中断

**解决**:
```powershell
# 重新安装 Electron
cd app\gui
pnpm install electron --force
```

### 6. Rust 编译错误

**问题**: Rust 组件编译失败

**解决**:
```powershell
# 更新 Rust 工具链
rustup update stable

# 重新构建
cargo clean
cargo build
```

---

## 性能优化建议

### 1. 增加内存分配

编辑 `.jvmopts` 文件（如果存在）：
```
-Xmx8G
-Xms8G
```

### 2. 使用 SSD

确保项目目录在 SSD 上，显著提升构建速度。

### 3. 并行构建

Bazel 默认并行构建，可以调整：
```
build --jobs=4
```

---

## 获取帮助

### 官方文档

- [项目文档](https://github.com/enso-org/enso/tree/develop/docs)
- [贡献指南](https://github.com/enso-org/enso/blob/develop/docs/CONTRIBUTING.md)
- [问题追踪](https://github.com/enso-org/enso/issues)

### 社区支持

- [Enso Community](https://community.ensoanalytics.com/)
- [Discord](https://discord.gg/enso)
- [Stack Overflow](https://stackoverflow.com/questions/tagged/enso)

---

## 版本信息

- **项目名称**: 玉皇大帝·数据分析平台
- **基于**: Enso Analytics
- **源码**: https://www.gitpp.com/liyihui/enso
- **文档版本**: 1.0
- **更新日期**: 2026-01-21

---

## 许可证

本项目采用开源许可证。详见 [LICENSE](LICENSE) 文件。
